import unittest,time
from HTMLTestRunner import HTMLTestRunner
import smtplib
import os
from email.mime.text import MIMEText
from email.header import Header


#定义发送邮件


























# test_dir='./'
# discover=unittest.defaultTestLoader.discover(test_dir,pattern='baidu_test*.py')
# report=open('./'+time.ctime()+'.html','wb')
# #定义测试报告
# runner=HTMLTestRunner(stream=report,title='baidusearch_report',description='the report of baidu search')
# runner.run(discover)
# report.close()
